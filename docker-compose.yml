services:
  marathon-app:
    build: .
    container_name: hangzhou-marathon
    restart: unless-stopped
    env_file: .env
    ports:
      - "${PUBLIC_PORT:-6080}:6000"
    volumes:
      - ./data:/app/data
